# Issue #5 focused UI evidence

`tasks-answer-nb-synthetic.png` is the single focused screenshot captured by
`apps/web/tests/monitor-smoke.mjs` on 2026-09-10. It uses intercepted synthetic
data and shows the Norwegian Oppdrag result with source disclosure. The same
smoke run passed Axe, keyboard/focus, nb/en and responsive checks without real
API requests.
