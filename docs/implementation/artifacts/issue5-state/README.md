# Issue #5 task-state recovery evidence

`task-states-nb-synthetic.png` is a focused Norwegian Bokmål desktop capture of
the server-derived ready-for-approval, incomplete and running representations.
All task names, IDs, people, accounts and source addresses are synthetic test
fixtures. The same browser run checks English, mobile and wall-panel widths,
keyboard/focus behavior and Axe without retaining a visual matrix.

The image documents the local fix candidate only. It is not evidence that the
fix is deployed or that the owner pilot has passed.
